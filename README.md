# AS_SETES_MARAVILHAS_DO_MUNDO_ANTIGO

- Será utilizado bootstrap ou css puro?
    Será utilizado bootstrap para ter um processo de desenvolvimento rápido e responsivo

- Quais são as regras para aplicar no CSS?
    Metodologia BEM (Block, Element, Modifier)
    Block (.block): Entidade independente (ex: .card, .nav).
    Element (.block__element): Parte de um bloco que não tem significado isolado (ex: .card__title).
    Modifier (.block--modifier): Uma flag que altera a aparência ou comportamento (ex: .card--featured).

    Nomenclatura: Utilizaremos predominantemente Hexadecimal para cores sólidas.
    Transparências: Caso precise de opacidade, utilizaremos RGBA.

- Quais são as regras para criar em javascript:
    Regras de Criação
    Variáveis: Utilize const por padrão. Use let apenas se a variável precisar de reatribuição. Nunca use var.
    Funções: Preferência por Arrow Functions para funções anônimas e callbacks. Funções nomeadas devem usar camelCase.
    Classes: Devem seguir o padrão PascalCase (ex: class ShoppingCart).
    Objetos: Devem ser declarados de forma literal e clara.

- Como o HTML será estruturado?
    Não será utilizado <br>
    Será utilizado uum layout fluido para melhor adaptação em diferentes dispositivos
    O projeto não será apenas a Mobile e Desktop. Utilizaremos uma estratégia de breakpoints no Bootstrap para garantir uma experiência fluida em diferetes telas
    
    Foi planejado um landingpage com as informações sobre as sete maravilhas do mundo antiogo 

    Cores do site:
    #04BF9D - PRIMARY
    #F27457 - SECONDARY
    #EFEFEF - BACKGROUND